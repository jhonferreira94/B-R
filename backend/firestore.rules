rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Todo acesso a dados passa pelas Cloud Functions (Admin SDK).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
